package ch02;

public class OperatorPriority {
    public static void main(String[] args) {
        // 대입연산
        // ++, -=, *=, /=, %=
        int number = 10;

        number += 10;
        // number = number + 10;
        number -= 10;
        // number = number - 10;
    }
}
